# frozen_string_literal: true

RSpec.describe Doku do
  it 'has a version number' do
    expect(Doku::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
